```text
知识库匹配结果(共匹配到7条信息,已用时0.30秒)
出处 [1] output123(1).md

注：

    最大可支持 8K Byte 容量标签的读写操作。标签根据不同型号有不同的容量值，具体可参考标签的数据手册。
    UID 数据区：RFID 标签的出厂 ID 存放区，数据为只读，UID 长度为 8 bytes，地址范围 0x000A~0x000D。
    地址 addr=0x000E 至 addr=0x4100 为用户数据寄存器，用户可对这些寄存器进行读写操作。

7. 操作流程
7.1 寻卡

CK-FR80 上电后的工作流程：
上电自动寻卡操作
7.2 读卡 UID

CK-FR80 读 UID 流程如下：

    打开设备电源。
    将卡片靠近读卡区域。
    设备自动读取卡片的 UID 信息。
    读取完成后，UID 信息将显示在屏幕上。

7.3 读卡数据区

CK-FR80 读电子标签数据区流程如下：

    确保设备已正确连接并开启。
    选择需要读取的电子标签数据区。
    执行读取操作，等待设备反馈结果。
    数据读取完成后，进行后续处理。 注意：具体操作步骤请参考设备使用手册。

7.4 写标签数据内容

CK-FR80 写标签数据流程如下： 1.
2.
3.
8. 数据例子
8.1 读电子标签的 UID

Tx: 00 B0 00 00 00 06 01 03 00 0A 00 04
Rx: 00 B0 00 00 00 0B 01 03 08 EF 1F 39 C0 50 01 04 E0
TX 数据解析如下：

出处 [2] output123(1).md
6. RFID 标签地址分配
表 5-1 RFID 标签数据地址分配表
区域	地址范围	属性
UID 地址	addr=0x000A	只读
	addr=0x000B	只读
	addr=0x000C	只读
	addr=0x000D	只读
用户数据区	addr=0x000E	读写
	addr=0x000F	读写
	addr=0x0010	读写
	addr=0x0011	读写
	...	读写
	addr=0x40FD	读写
	addr=0x40FE	读写
	addr=0x40FF	读写
	addr=0x4100	读写
注：		

    最大可支持 8K Byte 容量标签的读写操作。标签根据不同型号有不同的容量值，具体可参考标签的数据手册。

出处 [3] output123(1).md
Modbus TCP 开发手册
工业级读写器

V1.00 Date: 2018/08/08 产品开发手册
Manual 工业级读写器

V1.00 Date: 2018/08/
目录

    关于本文档
    MODBUS TCP 命令支持
    标签数据读写原理
    读卡寄存器分配
        4.1 寄存器定义
    RFID 标签地址分配
    操作流程
        6.1 寻卡
        6.2 读卡 UID
        6.3 读卡数据区
        6.4 写标签数据内容
    数据例子
        7.1 读电子标签的 UID
        7.2 读电子标签的数据区
        7.3 写电子标签的数据区
    附 MODBUS 返回通信错误码
        8.1 读错误码
        8.2 写错误码
    机械尺寸
        9.1 M12 圆形连接器选型
    免责声明
    修订历史

关于本文档

CK-FR08-E00 系列读卡器支持标准工业通讯协议 ModBus TCP，本文档主要向用户介绍
ModBus TCP 部分通讯协议应用。
2. MODBUS TCP 命令支持

CK-FR08-E00 对 ModBus TCP 协议命令的支持如下：

    0x03：读寄存器命令
    0x06：写单个寄存器
    0x10：写多个寄存器

出处 [4] output123(1).md
2. MODBUS TCP 命令支持

CK-FR08-E00 对 ModBus TCP 协议命令的支持如下：

    0x03：读寄存器命令
    0x06：写单个寄存器
    0x10：写多个寄存器

3. 可配置的回复格式

    错误码回复：操作标签时如果读写数据失败，返回 83H 和 90H+错误码
    正确回复：操作标签时如果读失败，返回数据 0，但需要通过判断寄存器地址的状态，确认数据有效
    注：仅版本 V2.3 以上有效，具体配置请查看“配置软件下的参数配置”

4. 标签数据读写原理

读卡器内部分数据输入区和数据输出区，PLC 向读卡器输入区写入相关的数据和操作
指令，读卡器执行读写器指令后，把执行指令的结果存储到读卡器的输出区。
5. 读卡寄存器分配

MODBUS 寄存器为 16bit 宽度，占用 2 bytes 内存空间，采用大端数据存储格式。
5. 寄存器定义

寄存器地址分配与功能定义见下表：
输入区寄存器分配表

出处 [5] output123(1).md
5. 读卡寄存器分配

MODBUS 寄存器为 16bit 宽度，占用 2 bytes 内存空间，采用大端数据存储格式。
5. 寄存器定义

寄存器地址分配与功能定义见下表：
输入区寄存器分配表
寄存器地址	寄存器名称	寄存器功能	R/W 特性
0x0000	系统信息寄存器	用于指示固件版本号和系统错误状态位	R
0x0001	标签读写状态寄存器	用于指示标签有效位、读完成、写完成位等	R
0x0002－0x0009	保留	暂未使用，读时为 0	R
0x000A~0x000D	标签 UID	获取标签的 UID 码	R
0x000E...	标签数据区	标签数据区跟标签空间有关	RW
系统信息寄存器

保存读卡器固件版本号以及错误信息。
Bit15 – bit8	Bit7 – bit0
保存版本号	表示系统错误信息

    版本号：当前程序配置的版本号。
    系统错误信息：代表系统异常，异常状态断电才允许清 0，否则要一直保持。

出处 [6] output123(1).md

    M12 连接器是一种工业标准的圆形连接器，广泛用于各种应用。
    选型时需要考虑以下因素：
        接口类型（A、B、C、D、X 等）
        传输介质（数据、信号、电源）
        防护等级（如 IP67、IP68）
        锁定机制（卡扣式、螺纹式） | 参数 | 描述 | |--------------|--------------------------| | 接口类型 | 定义连接器的应用场景 | | 传输介质 | 决定内部触点的设计 | | 防护等级 | 表示防尘防水能力 | | 锁定机制 | 确保连接的可靠性 | 注意：根据具体需求选择合适的 M12 连接器。

11. 免责声明

    开发预备知识
    CK-FR08-E00 系列模块将尽可能提供全面的开发模板、驱动程序和应用说明文档以方便用户使用，但也需要用户熟悉自己设计产品所采用的硬件平台及开发语言相关知识。
    EMI 与 EMC
    CK-FR08-E00 系列模块机械结构决定了其 EMI 性能必然与一体化电路设计有所差异。系列模块的 EMI 性能满足绝大部分应用场合，用户如有特殊要求，必须事先与我们联系。
    CK-FR08-E00 系列读卡器的 EMC 性能与用户底板的设计密切相关，尤其是电源电路、I/O 隔离、复位电路，用户在设计底板时必须充分考虑以上因素。我们将努力完善模块的电磁兼容性，但不对用户最终应用产品的 EMC 性能提供任何保证。

出处 [7] output123(1).md
8.2 读电子标签的数据区

Tx: 00 B4 00 00 00 06 01 03 00 0E 00 04 Rx: 00 B4 00 00 00 0B 01 03 08 1E 6F 6D 3C F3 F4 46 F4
TX 数据解析
内容	协议功能说明
00 B4	发送ID 计数器
00 00	协议号必须是0
00 06	后面的字节数
01	设备地址，没有使用
03	MODBUS 功能码
00 0E	寄存器地址
00 04	寄存器数量
RX 数据解析（读成功）
```
