#!/bin/bash

# 询问用户需要创建多少组文件
read -p "请输入要创建的组数 (n): " n

# 验证输入是否为数字
if ! [[ "$n" =~ ^[0-9]+$ ]]; then
    echo "错误：请输入有效的数字！"
    exit 1
fi

# 定义要写入的内容
content=''

# 循环创建文件并写入内容
for ((i=1; i<=n; i++))
do
    echo "$content" > "${i}.md"
    echo "已创建文件: ${i}.md"

done

echo "已完成创建 $n 组文件！"